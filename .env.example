# For network integration tests (optional — tests skip gracefully without these)
# Create a fine-grained PAT scoped to a private test repo with Contents read/write
GITHUB_TEST_TOKEN=
GITHUB_TEST_REPO=
